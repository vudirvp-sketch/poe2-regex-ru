# PoE2 Regex RU — Статус проекта

> **Репозиторий:** https://github.com/vudirvp-sketch/poe2-regex-ru
> **Онлайн:** https://vudirvp-sketch.github.io/poe2-regex-ru/

---

## Текущая итерация: 43 — D3 pre-analysis: усечение основ

### Резюме

Path D полностью реализован и **production-verified** (iter 41). Char-limit diagnostic реализован (iter 42). В iter 43 проведён **предварительный анализ** усечения основ (D3): выявлены 95 опасных пар словоформ, классифицированы паттерны аффиксов (8 типов), сформулированы правила безопасного усечения. **Код не изменён** — только анализ и документация.

### Ключевые выводы iter 43 (D3 pre-analysis)

1. **8 структурных паттернов** аффиксов: `##% текст` (234), `Текст ##% текст` (152), `+## текст` (35), `(##)% текст` (16), без чисел (47) и др.
2. **95 опасных пар** — где усечение одного слова совпадает с другим словом из другого семейства модов
3. **5 типов опасностей**: падежные формы (A), разные слова с общим префиксом (B), пунктуация (C), число-текст (D), дефисные (E)
4. **Главное правило**: усечение безопасно ⇔ усечённая форма не появляется ни в одном rawText из другой familyKey в той же категории
5. **Текущий `tryWordTruncation`** уже делает усечение + exclude, но не проверяет FP от совпадения усечённых основ с другими словоформами
6. **Реальная выгода от D3** — marginal в большинстве случаев: полный суффикс часто уже уникален, усечение + exclude редко короче

### План реализации D3 (на основе анализа)

Расширить `tryWordTruncation` шагом: после генерации усечённого кандидата, если `isSuffixUniqueInCategory` возвращает false — вызвать `computeExcludePatterns` для усечённого кандидата и сравнить `totalLen(truncated + excludes)` vs `totalLen(full + currentExcludes)`. Минимальное изменение, закрывающее D3.

### Детерминированная стратегия регексов (8 принципов)

| # | Принцип | Описание |
|---|---------|----------|
| 1 | **One Mod = One Quoted Group** | Каждый выбранный мод → одна quoted group |
| 2 | **Multi-Mod = AND Across Blocks** | N модов → N quoted groups через пробел (same-block AND confirmed iter 41) |
| 3 | **`\|` Scope — TOP LEVEL** | `\|` работает только на верхнем уровне одного quoted group |
| 4 | **`.*` Bridging Within Single Block** | `"prefix.*suffix"` мостит число и слова в одном блоке |
| 5 | **Suffix Uniqueness** | Кратчайший suffix, уникальный для мода в категории (≥3 значимых символов на слово) |
| 6 | **Shared Suffix → Number Enumeration** | `"(1[0-5])%.*suffix"` |
| 7 | **Cross-Block FP Risk** | `"X" "Y"` может-match разные блоки → использовать `.*` bridge |
| 8 | **Same-Family OR → Path D** | `"prefix.*A\|prefix.*B\|prefix.*C"` — production-verified (iter 41). Constraint: ≤250 chars |

### Подтверждённые ограничения PoE2

| Синтаксис | Работает? | Примечание |
|-----------|-----------|------------|
| `\|` между одиночными словами | ✅ | `"Бездн\|Делир"` |
| `\|` top-level + `.*` мосты (Path D) | ✅ | iter 38-41 |
| `\|` между quoted groups | ❌ | zero matches |
| `\|` многословный внутри `()` | ❌ | nothing matches |
| Пробел = AND (same-block + cross-block) | ✅ | iter 41 |
| `.*` внутри одного блока | ✅ | |
| `(?!…)` per-block | ✅ | |
| `!` item-wide negation | ✅ | |
| `^` start-of-block anchor | ✅ | |
| Number enumeration | ✅ | |
| Regex char limit ≈ 250 chars | ⚠️ | diagnostic iter 42 |

### Path D — финальный статус

| Шаг | Статус |
|-----|--------|
| D1 In-game test | ✅ iter 39 |
| D2 ETL + Phase D | ✅ iter 40 |
| D3 regexExclude усечённые основы | ⏳ pre-analysis iter 43, реализация — след. итерация |
| D4 Runtime совместимость | ✅ iter 40 |
| D5 Production verification | ✅ iter 41 |
| D6 Все категории | ✅ iter 41 |
| D7 Char-limit diagnostic | ✅ iter 42 |

### Опасные пары словоформ для усечения (топ-10 из 95)

| Короткая | Длинная | Контекст |
|----------|---------|----------|
| `самострела` | `самострелами` | перезарядка vs урон/скорость |
| `атак` | `атакам/атаками/атаки` | от атак vs к атакам vs атаками |
| `удар` | `удара/ударами/ударов` | наносили удар vs критического удара |
| `чар` | `чарами` | сотворения чар vs чарами |
| `снаряд` | `снарядами/снарядов` | дополнительный снаряд vs урон снарядами |
| `силе` | `силе,` | к силе vs к силе, ловкости |
| `при` | `приказов/приспешников/призраков` | 7+ разных корней на «при» |
| `эффект` | `эффекта/эффективности/эффектов` | эффект архонта vs эффективности |
| `свойств` | `свойствами/свойств-префиксов` | добавленных свойств vs свойств-префиксов |
| `оберег` | `оберега/оберегов` | активен оберег vs эффекта оберега |

### Известные проблемы

| # | Issue | Impact | Status |
|---|-------|--------|--------|
| 1 | **regexExclude: усечённые основы** — FP при совпадении с другой словоформой | MEDIUM | OPEN (D3, plan ready) |
| 2 | **Симулятор `(?!…)`** — не моделирует per-block semantics | LOW | OPEN |
| 3 | **PoE2 regex char limit ≈ 250 chars** — 2 over-limit entries в jewel | MEDIUM | DIAGNOSTIC iter 42 |

### SEO-статус: ✅ полный набор реализован

---
Контакты: Discord **woonderdad**
