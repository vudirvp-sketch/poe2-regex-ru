# PoE2 Regex RU — План доработок

> **Версия:** 11.0 | **Дата:** 2026-06-08
> **Репозиторий:** https://github.com/vudirvp-sketch/poe2-regex-ru

---

## Текущий статус (Session 68)

### Выполнено
- ✅ **Фаза 0-9:** Regex Oracle, number-regex fix, Trie/DP factorization, dialect optimizations, iterative optimizer, AND-composed regex, word truncation, regexPrefixContext
- ✅ **Session 50-53:** Oracle validation, repairCrossFamilyFP(), patchOptimizationEntries(), ETL re-run
- ✅ **Session 56:** VirtualizedModList, dual-slot ranges, jewel sub-headers
- ✅ **Session 57:** Все tablet cross-family FP устранены (4→0)
- ✅ **Session 59:** Bug fixes: clearSelections, toggleToken cleanup, auto URL sync, ProfilePanel restore, VendorPage searchLogic
- ✅ **Session 60:** OR-suffix RANGE grouping, selection count fix, compiler `()` wrapping
- ✅ **Session 61:** P2 optimizer collapse indicator, UI audit bug fixes (keyboard a11y, VendorPage URL sync, VendorChip click target, 404 route, NaN/negative validation, clearAll reset)
- ✅ **Session 62:** UI audit — VendorChip ARIA fix + duplicate interface removal + negative validation, RegexOutput aria-valuenow clamp, PageStateWrapper ARIA roles, ProfilePanel delete confirmation + aria-labels, CategoryControlPanel arrow key navigation, VirtualizedModList unused prop cleanup
- ✅ **Session 64:** ProfilePanel duplicate name prevention, Ctrl+Shift+C→X shortcut, onBlur race condition fix, VirtualizedModList re-measure, dual-number aria labels
- ✅ **Session 65:** JewelPage hidden mods indicator + deselect, step={1} on all number inputs, RegexOutput copy failure feedback, documentation cleanup
- ✅ **Session 66:** Jewel classification heuristic improved (76%→96%), FilterChip min-w-[30%] removed, number regex [0-9]→. confirmed NOT VIABLE
- ✅ **Session 67:** Jewel heuristic все 9 mismatches исправлены (96%→100%). Типографские исправления (кинджал→кинжал), ES/marker/dagger/shield/banner override and scoring fixes. 45 unit tests добавлены.
- ✅ **Session 68:** ETL pipeline аудит — 1823/1823 valid, 0 cross-family FP. i18n-overrides все 56 актуальны. filterTokensByJewelType(::origin) — бага нет. dp-factorizer/trie-factorizer — не мёртвый код (ETL-only). constants.ts удалён (0 импортов). iterative-optimizer dry-run корректен. Unused imports в тестах почишены.

### Результаты ETL (Session 57)
| Категория | Токенов | Cross-family FP |
|-----------|---------|-----------------|
| amulet | 428 | 0 |
| belt | 298 | 0 |
| jewel | 193 | 0 |
| jewel-corrupted | 10 | 0 |
| jewel-desecrated | 47 | 0 |
| relic | 58 | 0 |
| ring | 369 | 0 |
| tablet | 82 | 0 |
| waystone | 311 | 0 |
| waystone-desecrated | 27 | 0 |
| **Итого** | **1823** | **0** |

---

## Следующие шаги

### P3: ETL & Code Audit — ✅ ЗАВЕРШЕНО (Session 68)
- ETL pipeline: `pnpm etl` отрабатывает корректно, 1823 tokens, 0 cross-family FP
- Cross-validation: 543/543 tests pass
- i18n-overrides: все 56 актуальны, все токены найдены в JSON
- filterTokensByJewelType: `::origin` suffix НЕ попадает в `token.familyKey.ru`, бага нет
- dp-factorizer/trie-factorizer: НЕ мёртвый код — используются ETL скриптами (compute-optimizations, iterative-optimizer, run-etl)
- constants.ts: удалён (нигде не импортировался)
- iterative-optimizer: dry-run подтверждает корректность (0 FN, корректная конвергенция)
- buildAstFromSelections: regexPrefixContext/regexExclude grouping корректна (context only when all tokens share same; exclude union by design)
- Unused imports в mod-classifier.test.ts почишены

### P1: Browser-тестирование (ручное)
- Запустить `pnpm dev`, проверить все страницы
- Скролл, поиск, фильтры, клики по чипам
- Per-token ranges, dual-slot ranges
- Jewel type sub-headers + hidden mods indicator
- ProfilePanel (save/load/delete/rename + delete confirmation)
- VendorPage (chips, numeric inputs, URL sync)
- CategoryControlPanel (arrow key navigation в радиогруппах)

### P2: Jewel classification heuristic ~~(remaining edge cases)~~ — ✅ ЗАВЕРШЕНО
- ~~9 оставшихся mismatches (95.7% → target 97%+)~~ — Все исправлены, accuracy = 100%
- Исправлены: shield defence, кинжал typo, minion resist lookbehind, ES threshold narrowing, triple-ailment, mark scoring, banner е/ё, conditional stun threshold

### LOW: Mobile-specific testing
- Touch targets, scroll behavior (нужен реальный девайс)

### Баг-профиль для будущего аудита
При дальнейшем аудите искать:
1. **Copy failure feedback** — ✅ Исправлено Session 65
2. **Дробные числа** — ✅ Исправлено Session 65
3. **Number regex [0-9] → .** — ✅ НЕ РЕАЛИЗУЕМО: `.` в PoE2 regex = любой символ, не только цифры
4. **Edge cases с OR-suffix RANGE** — при разных (min,max) но одинаковых суффиксах, объединение может быть некорректным
5. **VendorPage numeric-only без чекбокса** — свойство с numericInput, но без чекбокса (selectedIds), может не попасть в regex
