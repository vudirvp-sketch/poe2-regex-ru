# PoE2 Regex RU — План доработок

> **Версия:** 6.0 | **Дата:** 2026-06-07

---

## Текущий статус проекта

- **8 страниц категорий** + VendorPage
- **238 тестов** — все проходят
- **1,560 токен** в 10 категориях данных
- **57 i18n overrides** применено
- **CI/CD** (GitHub Actions: ETL → Build → Deploy на GitHub Pages)
- **Number regex исправлен** — `[0-9]` вместо `.` во всех числовых паттернах
- **Regex Oracle создан** — валидация регексов на FP/FN
- **jewel-desecrated в STRICT_CATEGORIES** — MIN_REGEX_LEN=10

---

## Оставшиеся задачи

### 🔴 P0 — Критично

1. **Push фиксов в GitHub** — `git push` нужных фиксов
2. **Re-run ETL** — `pnpm etl` чтобы перегенерировать JSON с новыми `[0-9]` паттернами
3. **In-game тесты** — Группы A-G из IN_GAME_TESTS.md

### 🟡 P1 — Важно

4. **Фаза 2: Trie-факторизация** — See `OPTIMIZER_PLAN.md`
5. **Cross-category conflicts** — 4002 конфликта. Oracle теперь может их детектить
6. **jewel-corrupted** — avg regex len = 7.4, добавить в STRICT_CATEGORIES
7. **Number regex length increase** — проверить что RANGE регексы не превышают 250 после ETL

### 🟢 P2 — Желательно

8. **Фазы 3-8 оптимизатора** — DP на Trie, диалектные оптимизации, итеративный цикл
9. **Jewel classification accuracy** — ~84% со scoring
10. **Виртуализация списков** (belt 298, ring 366, amulet 427)

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Итерация 32 выполнена. Фаза 0 (Regex Oracle) и Фаза 1 (number-regex fix) реализованы.
238 тестов проходят, pnpm build проходит.

Остановился на:
- P0: git push фиксов в main
- P0: pnpm etl (перегенерация JSON с новыми [0-9] паттернами)
- P0: In-game тесты Групп A-G из docs/IN_GAME_TESTS.md
- P1: Фаза 2: Trie-факторизация (см. OPTIMIZER_PLAN.md)
- P1: Проверить что RANGE регексы не превышают 250 после ETL

Ключевые файлы:
- src/core/regex-oracle.ts — НОВЫЙ: валидатор регексов
- src/core/number-regex.ts — ИСПРАВЛЕН: [0-9] вместо .
- scripts/run-etl.ts — ОБНОВЛЁН: --validate флаг
- OPTIMIZER_PLAN.md — план оптимизатора

Команды: cd poe2-regex-ru && pnpm install && pnpm build && npx vitest run --root .
```
