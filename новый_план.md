# PoE2 Regex RU — План доработок

> **Версия:** 5.0 | **Дата:** 2026-06-06

---

## Текущий статус проекта

- **8 страниц категорий** + VendorPage
- **213 тестов** — все проходят
- **1,560 токен** в 10 категориях данных
- **57 i18n overrides** применено
- **CI/CD** (GitHub Actions: ETL → Build → Deploy на GitHub Pages)
- **`\d` → `[0-9]`** — безопасная замена в number-regex.ts
- **jewel-desecrated в STRICT_CATEGORIES** — MIN_REGEX_LEN=10

---

## Оставшиеся задачи

### 🔴 P0 — Критично

1. **Push фиксов в GitHub** — `git push` нужных фиксов
2. **Re-run ETL** — `pnpm etl` чтобы перегенерировать jewel-desecrated с новыми MIN=10
3. **In-game тесты** — Группа A (AND/OR/NOT) из IN_GAME_TESTS.md

### 🟡 P1 — Важно

4. **Cross-category conflicts** — 4002 конфликта (регис/analysis-report.md). Большинство benign для аксессуаров, но короткие типа "молнии" — проблема
5. **jewel-corrupted** — avg regex len = 7.4, рассмотреть добавление в STRICT_CATEGORIES
6. **Icon proportions** — relic/vendor/belt PNG padding
7. **Per-token dual-number RANGE filtering** — Второй placeholder не поддерживается
8. **HomePage hardcoded mod counts** — Устаревшие числа

### 🟢 P2 — Желательно

9. **Jewel classification accuracy** — ~84% со scoring, улучшить static lookup
10. **Виртуализация списков** (belt 298, ring 366, amulet 427)
11. **TabletPage PageStateWrapper** — Ещё inline pattern

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Итерация 31 выполнена. \d→[0-9] заменён, jewel-desecrated в STRICT_CATEGORIES (MIN=10),
IN_GAME_TESTS.md переписан с hypothesis-based тестами, скрипт анализа создан,
документация почищена.

Остановился на:
- P0: git push фиксов в main
- P0: pnpm etl (перегенерация JSON с новыми MIN=10 для jewel-desecrated)
- P0: In-game тесты Группы A (AND/OR/NOT) из docs/IN_GAME_TESTS.md
- P1: jewel-corrupted avg regex len = 7.4 (кандидат в STRICT_CATEGORIES)
- P1: Кросс-категорийные конфликты (см. регис/analysis-report.md)

Ключевые файлы:
- scripts/etl/compute-regex.ts — STRICT_CATEGORIES, MIN_REGEX_LEN_STRICT=10
- src/core/number-regex.ts — [0-9] вместо \d
- scripts/analyze-regexes.ts — анализ качества регисов
- регис/analysis-report.md — отчёт по анализу
- docs/IN_GAME_TESTS.md — переписанные тесты (Groups A-F)

Команды: cd poe2-regex-ru && pnpm install && pnpm build && npx vitest run --root .
```
