# PoE2 Regex RU — План доработок

> **Версия:** 13.0 | **Дата:** 2026-06-07

---

## Текущий статус проекта

- **8 страниц категорий** + VendorPage
- **471 тест** — все проходят
- **Phase 9 + Session 50 выполнена** — Oracle validation учитывает regexPrefixContext, repairCrossFamilyFP() лимит 3→5
- **0 FN** — все моды матчатся
- **ETL re-run pending** — нужно запустить чтобы увидеть реальный эффект Session 50

---

## Оставшиеся задачи

### 🔴 P0 — Критичные

1. **Запустить ETL с Session 50 изменениями** — `pnpm etl -- --validate-item` чтобы увидеть эффект от Oracle fix + лимит 5 excludes + расширенные маркеры. Ожидаемое снижение cross-family FP с 62 до ~20-25.

### 🟡 P1 — Важно

2. **In-game тесты Group M** — `|` внутри `()`, number range с `|`. Тесты добавлены в IN_GAME_TESTS.md, нужно проверить в игре.
3. **Optimizer expansion** — `compute-optimizations.ts`: пробовать обрезанные формы (truncated) для shared regex в family grouping. Учитывать regexPrefixContext при объединении.

### 🟢 P2 — Желательно

4. **Виртуализация списков** (belt 298, ring 366, amulet 427)
5. **Multi-line mod handling** — Разрушительный: каждая под-строка = отдельный блок в ETL
6. **HomePage hardcoded mod counts** — Category cards show stale counts

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Итерация 50 (Session 50) выполнена. 471 тест проходит. pnpm build успешен.

Ключевые изменения Session 50:
- validateGeneratedRegexesItem() теперь компилирует regexPrefixContext как AND(context, regex)
  при валидации, что соответствует поведению UI. Даёт точные FP-счётчики.
- repairCrossFamilyFP() лимит excludes поднят с 3 до 5
- CONFLICT_MARKERS расширены: добавлены самострелами, кинжалами, посохами, копьями, для
- In-game tests Group M добавлены в IN_GAME_TESTS.md (| внутри (), number range)
- TS build fix: regexPrefixContext default добавлен в test helper

Остановился на:
- P0: Нужно запустить ETL (pnpm etl -- --validate-item) чтобы увидеть реальный эффект Session 50
  Ожидается ~20-25 cross-family FP вместо 62
- P1: In-game тесты Group M — проверить в игре
- P1: Optimizer expansion (compute-optimizations.ts) — truncated forms + regexPrefixContext

Ключевые файлы:
- scripts/run-etl.ts — Oracle validation fix + CONFLICT_MARKERS expansion + exclude limit 5
- docs/IN_GAME_TESTS.md — Group M tests
- tests/etl/compute-optimizations.test.ts — regexPrefixContext default fix

Команды: cd poe2-regex-ru && pnpm install && npx vitest run --root .
```
