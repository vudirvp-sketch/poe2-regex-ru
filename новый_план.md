# PoE2 Regex RU — План доработок

> **Версия:** 9.0 | **Дата:** 2026-06-07

---

## Текущий статус проекта

- **8 страниц категорий** + VendorPage
- **452 тестов** — все проходят
- **Блок-модель матчинга реализована** — `matchPoE2RegexItem()` + `getItemSearchBlocks()`
- **`getItemSearchText()` УДАЛЁН** — все тесты переведены на блок-модель (Session 42)
- **In-game тесты Phase 7 завершены** — 13 блоков, ~30 тестов, результаты интегрированы
- **`.*` НЕ пересекает границы блоков** — фундаментальное открытие
- **Описание НЕ индексируется** — `description` field добавлен
- **`?` НЕ работает** в PoE2 regex

---

## Оставшиеся задачи

### 🟡 P1 — Важно

1. **Re-run ETL** — `pnpm etl` чтобы перегенерировать JSON с jewel-corrupted STRICT
2. **После ETL: проверить jewel-corrupted regex** — должны быть ≥7 символов, без `()`
3. **После ETL: проверить waystone implicits** — не должны появиться в данных (не аффиксы)
4. **Интеграция `batchValidateItem()` в ETL** — добавить флаг `--validate-item` в `run-etl.ts`

### 🟢 P2 — Желательно

5. **Фазы 3-8 оптимизатора** — DP на Trie, диалектные оптимизации, итеративный цикл
6. **Виртуализация списков** (belt 298, ring 366, amulet 427)
7. **Multi-line mod handling** — Разрушительный: каждая под-строка = отдельный блок в ETL

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Итерация 42 (getItemSearchText Removal) выполнена. 452 тестов проходят.
Ключевые изменения:
- getItemSearchText() УДАЛЁН из poe2-regex-matcher.ts
- ~81 вызов в тестах заменены на matchPoE2RegexItem() / getItemSearchBlocks().join('\n')
- H4 cross-block .* тест исправлен: было true (concat), стало false (block-based, корректно)
- Неиспользуемые импорты почищены

Остановился на:
- P1: Re-run ETL (pnpm etl) с jewel-corrupted STRICT
- P1: После ETL — проверить jewel-corrupted regex (≥7 символов, без ())
- P1: После ETL — проверить что waystone implicits не появились в данных
- P1: Интеграция batchValidateItem() в ETL pipeline (--validate-item флаг)

Ключевые файлы:
- src/core/poe2-regex-matcher.ts — getItemSearchText() УДАЛЁН
- tests/core/hypothesis-patterns.test.ts — все вызовы заменены на block-based
- tests/core/poe2-regex-matcher.test.ts — Vendor секция переведена на matchPoE2RegexItem()
- scripts/run-etl.ts — нужно добавить --validate-item флаг
- src/core/regex-oracle.ts — batchValidateItem() уже есть

Команды: cd poe2-regex-ru && pnpm install && npx vitest run --root .
```
