# PoE2 Regex RU — Контекст для продолжения в новом чате

> **Дата:** 2026-06-07 | **Сессия:** 52

---

## Текущий статус проекта

- **479 тестов** — все проходят
- **pnpm build** — успешен
- **Session 52 выполнена** — Runtime optimizer использует regexPrefixContext/regexExclude из OptimizationEntry
- **0 FN** — все моды матчатся
- **3 cross-family FP** — jewel.mod_am4lla (fix pending ETL re-run), tablet x2 (accepted)

---

## Остановился на

- ✅ **P1 (Runtime optimizer + regexPrefixContext/regexExclude)** — ВЫПОЛНЕНО
  - `applyOptimizationTable()` создаёт `AND(LITERAL(context), LITERAL(regex))` и `EXCLUDE` ноды
  - `findLiteralsInOr()` находит AND-wrapped LITERALs
  - Dedup обрабатывает AND-wrapped nodes
- ⬜ **P1: ETL re-run** — нужно запустить `pnpm etl -- --validate-item` локально для подтверждения jewel.mod_am4lla fix
- ⬜ **P1: In-game тесты Group M** — проверить `|` внутри `()` в PoE2
- ⬜ **P1: Truncated forms в compute-optimizations.ts** — Strategy 1e для shared regex
- ⬜ **P2: List virtualization** — belt/ring/amulet
- ⬜ **P2: HomePage hardcoded counts**

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Сессия 52 выполнена. 479 тестов проходит. pnpm build успешен.

Ключевые изменения Session 52:
- Runtime optimizer (src/core/optimizer.ts) теперь использует regexPrefixContext/regexExclude
  из OptimizationEntry. buildOptimizedNode() создаёт AND(context, regex, EXCLUDE) структуру.
- findLiteralsInOr() находит AND-wrapped LITERALs (токены с per-token context/excludes).
- deduplicateOrGroups() обрабатывает AND-wrapped nodes — собирает tokenIds, создаёт dedup: prefix.
- Добавлен approxCompiledLength() для точного расчёта экономии при оптимизации.
- Удалены устаревшие файлы из корня: JewelPage.tsx, ModList.tsx, TabletPage.tsx, mod-classifier.ts.

Остановился на:
- P1: Нужно запустить ETL (pnpm etl -- --validate-item) чтобы подтвердить jewel.mod_am4lla fix
  (exclude limit поднят до 8 в Session 51). Ожидается 1-2 cross-family FP вместо 3.
- P1: In-game тесты Group M — проверить | внутри () в PoE2
- P1: Truncated forms в compute-optimizations.ts Phase A
- P2: List virtualization, HomePage counts

Ключевые файлы:
- src/core/optimizer.ts — regexPrefixContext/regexExclude support + AND-wrapped LITERAL handling
- tests/core/optimizer.test.ts — 8 новых тестов для context/excludes optimization
- AGENT_NAVIGATION.md — v52.0
- worklog.md — актуальный

Команды: cd poe2-regex-ru && pnpm install && npx vitest run --root .
```
