# iter 124 — Files to DELETE from your local checkout
#
# This instruction-list file is stale: the file it instructed you to
# delete (DELETIONS-iter123.txt) was already deleted in iter 124
# itself. iter 124 cleans up the instruction list itself (same pattern
# iter 121 used to delete DELETIONS-iter100.txt, and iter 123 used to
# delete DELETIONS-iter{121,122}.txt).
#
# Run from your repo root:
#
#   rm DELETIONS-iter123.txt
#
# Or, if you extracted this archive over your checkout (which already
# replaced the modified docs), the DELETIONS-iter123.txt file will
# still be on disk because zip extract doesn't delete files. So you
# need to run this rm command manually.
#
# After deletion, you can also delete this DELETIONS-iter124.txt file
# itself — it is only needed once, at archive-apply time.

DELETIONS-iter123.txt
