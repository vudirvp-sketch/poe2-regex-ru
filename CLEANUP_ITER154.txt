# iter 154 — Cleanup manifest

> Применить локально: extract архива iter154 в корень репозитория, затем выполнить `git rm` для файлов ниже (или `git rm --cached` для untrack-only).

## 1. Root files — DELETE (6 files)

Старые archive-manifests и instructions из iter 126/133/143. Info уже в git history + worklog.

```bash
git rm DELETIONS.txt
git rm DELETIONS-iter126.txt
git rm MANIFEST.txt
git rm README_ITER143_FEEDBACK.txt
git rm README-iter126.md
git rm iter143-feedback.patch
```

## 2. One-shot scripts (iter 99–128) — DELETE (11 files + 1 empty folder)

Все скрипты уже применили свои патчи к данным (logic либо inlined в tests, либо данные уже в JSON). Удаление безопасно — нет code deps.

```bash
git rm scripts/apply-ki12-fix.py
git rm scripts/apply-ki13-fix.py
git rm scripts/audit-ambiguous-suffixes.py
git rm scripts/audit-tier-hardcoded-regex.py
git rm scripts/audit_block_sort_coverage.py
git rm scripts/verify-iter99-alpha-sort.ts
git rm scripts/audit/audit-iter108-compliance.ts
git rm scripts/apca_iter110_results.txt
git rm scripts/apca_iter111_results.txt
git rm scripts/apca_validate_iter110.py
git rm scripts/apca_validate_iter111.py
rmdir scripts/audit  # пустая папка после удаления audit-iter108-compliance.ts
```

## 3. `.etl-cache/*.html` — UNTRACK ONLY (11 files, 4.3 MB)

Файлы попали в git-индекс ДО того, как `.etl-cache/` был добавлен в `.gitignore`. Untrack убирает их из git, но локальные файлы остаются (можно re-fetch из cache при ETL re-run).

```bash
git rm --cached .etl-cache/poe2db_tw_ru_Amulets.html
git rm --cached .etl-cache/poe2db_tw_ru_Belts.html
git rm --cached .etl-cache/poe2db_tw_ru_Emerald_ModifiersCalc.html
git rm --cached .etl-cache/poe2db_tw_ru_Jewels.html
git rm --cached .etl-cache/poe2db_tw_ru_Rings.html
git rm --cached .etl-cache/poe2db_tw_ru_Ruby_ModifiersCalc.html
git rm --cached .etl-cache/poe2db_tw_ru_Sapphire_ModifiersCalc.html
git rm --cached .etl-cache/poe2db_tw_ru_Seal_Relic.html
git rm --cached .etl-cache/poe2db_tw_ru_Tablet.html
git rm --cached .etl-cache/poe2db_tw_ru_Urn_Relic.html
git rm --cached .etl-cache/poe2db_tw_ru_Waystones.html
```

Или одной командой:

```bash
git rm --cached .etl-cache/*.html
```

## 4. Файлы, оставленные в repo (НЕ удалять)

- `регис/` (11 Russian source files) — referenced из `scripts/etl/i18n-overrides.json` source field + `src/shared/mod-classifier.ts` line 1222. Source material для ETL.
- `scripts/patch-ki10-ki12-overrides.ts` (iter 153 one-shot patcher) — оставлен до следующего ETL refresh для повторного применения если `manualOverride` flag не сработает.
- `package-lock.json` — несмотря на `.gitignore` "project uses pnpm only", untrack может сломать user's local env если есть npm. Оставлен для safety.
- `docs/ITER142_PROPOSALS.md`, `docs/ITER148_TOOLBAR_REFACTOR.md`, `docs/UI_AUDIT.md`, `docs/UI_REFACTOR_PLAN.md`, `docs/UI_VISUALIZATION_AUDIT.md`, `docs/AFFIXES_GROUPING_ANALYSIS.md`, `docs/AFFIX_ORDERING_PLAN.md` — historical planning docs, referenced из AGENT_NAVIGATION.md Section 13. Refs к удалённым scripts обновлены.

## 5. Verification после cleanup

```bash
pnpm install   # если ещё не сделано
pnpm exec tsc -b           # 0 errors
pnpm exec eslint .         # 0 warnings
pnpm test                  # 2235/2235 PASS (без code changes, только docs + script deletions)
pnpm exec vite build       # PASS, main bundle 342 KB
```

## 6. Изменённые файлы (для merge)

Архив `poe2-regex-ru-iter154-cleanup.tar.gz` содержит:

- `STATUS.md` (modified) — iter 154, 3 KI closed, cleanup summary
- `worklog.md` (modified) — iter 154 entry, iter 153 compressed
- `AGENT_NAVIGATION.md` (modified) — header iter 153→154, Pitfalls 38/39/55 + Section 1 updated
- `tests/core/iter127-ki12-tier-hardcoded-regex.test.ts` (modified) — 2 комментария про удалённый audit script
- `docs/AFFIX_ORDERING_PLAN.md` (modified) — 4 ref'а к удалённому `audit_block_sort_coverage.py` обновлены
- `docs/UI_AUDIT.md` (modified) — 1 ref к удалённому `apca_validate_iter110.py` обновлён
- `CLEANUP_ITER154.txt` (NEW) — этот manifest
