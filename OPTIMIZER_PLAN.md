# PoE2 Regex RU — План реализации итеративного оптимизатора регексов

> **Версия:** 1.2 | **Дата:** 2026-06-07
> **Назначение:** Этот файл — инструкция для языковой модели / агента. Агент получает этот файл + ссылку на репозиторий и реализует шаги последовательно, останавливаясь на контрольных точках.
> **Репозиторий:** https://github.com/vudirvp-sketch/poe2-regex-ru

---

## Текущий статус (итерация 34)

### Выполнено
- ✅ **Фаза 0: Regex Oracle** — `src/core/regex-oracle.ts` + 25 тестов + `--validate` флаг в ETL
- ✅ **Фаза 1: Исправление number-regex.ts** — `.` → `[0-9]` во всех числовых паттернах (30 тестов обновлено)
- ✅ **ETL перегенерация JSON** — `pnpm etl` выполнен с `[0-9]` паттернами, все 10 категорий
- ✅ **ETL --validate** — Oracle-валидация: FP=1054 (ожидаемо), FN=73 (реальные баги)
- ✅ **Проверка лимита 250** — ни один regex не превышает 250 символов
- ✅ **Фаза 2: Trie-факторизация** — `src/core/trie-factorizer.ts` + 40 тестов
- ✅ **Фаза 3: DP на Trie** — `src/core/dp-factorizer.ts` + 40 тестов
- ✅ **Фаза 4 (частично): Оптимизации диалекта PoE2** — ёфикация `[её]`, окончания `[юя]` внутри dp-factorizer

### Не начато
- ⬜ Фаза 4 (доп): Интеграция диалектных оптимизаций в ETL пайплайн
- ⬜ Фаза 5: Итеративный цикл оптимизации
- ⬜ Фаза 6: Интеграция Trie/DP в ETL пайплайн
- ⬜ Фаза 7: Игровые тесты
- ⬜ Фаза 8: Финальная полировка
- ⬜ Исправление FN багов (73 false negatives в Oracle)

---

## 0. Контекст и терминология

### 0.1 Что делает проект сейчас

Проект генерирует регулярные выражения для поиска модификаторов предметов в Path of Exile 2 (PoE2). Пайплайн:

```
fetch-poe2db.ts → normalize.ts → compute-regex.ts → compute-optimizations.ts → optimizer.ts → compiler.ts → "регекс строка"
```

- **compute-regex.ts** — для каждого токена (модификатора) вычисляет минимальный уникальный подстроку. Работает по шаблонам: берёт текст после последнего `##`/`#` (суффикс шаблона), укорачивает слева пока уникально. Если суффикс не уникален — удлиняет (Strategy 1b), берёт вторую стату (1c), или падает обратно на substring search (Strategy 2).
- **compute-optimizations.ts** — группирует токены по familyKey (нормализованный шаблон), находит общий регекс для семьи, ищет cross-family общие подстроки через LCS.
- **optimizer.ts (runtime)** — дедуплицирует одинаковые регексы в OR-группах, применяет таблицу оптимизаций (заменяет набор LITERAL на один общий).
- **regex-oracle.ts** — валидирует регекс против набора целевых и исключаемых текстов, проверяет лимит 250 символов.
- **trie-factorizer.ts** — строит Trie, находит общие префиксы/суффиксы, факторизует наборы строк.

### 0.2 Чего НЕ хватает — ДАЛЬНЕЙШАЯ ФАКТОРИЗАЦИЯ

Trie-factorizer создан (Фаза 2), но ещё НЕ интегрирован в ETL пайплайн. Следующие шаги:

1. **DP на Trie** — выбрать оптимальные точки факторизации через динамическое программирование
2. **Учитывать диалект PoE2** при оптимизации — ёфикация `[её]`, опциональные окончания `?`
3. **Интеграция в ETL** — заменить/дополнить compute-optimizations.ts факторизацией
4. **Исправить FN баги** — 73 false negatives в Oracle-валидации

### 0.3 Диалект PoE2 regex — проверенные в игре факты

| Фича | Поведение | Влияние на оптимизатор |
|------|-----------|----------------------|
| `.` | Матчит **ЛЮБОЙ** символ | Использовать с осторожностью — Oracle проверяет FP |
| `[0-9]` | Матчит только цифру | Используется вместо `.` в числовых паттернах (Фаза 1 выполнена) |
| `!` | NOT — скрывает предметы | — |
| `.*` | Пересекает границы модов | prefix anchoring критичен |
| `\|` | OR — стандартное поведение | — |
| `()` | Группировка | Для факторизации |
| `[]` | Класс символов | `[её]` для ёфикации, `[юя]` для окончаний |
| `?` | Опциональность | Полезно для опциональных окончаний |
| Регистр | Case-insensitive поиск | Все регексы в нижнем регистре |
| Лимит | **250 символов** на один регекс | Оптимизатор должен следить за лимитом |

---

## 1. Фаза 0: Валидатор (Oracle) ✅ ВЫПОЛНЕНА

## 2. Фаза 1: Исправление числовых паттернов ✅ ВЫПОЛНЕНА

## 3. Фаза 2: Trie-факторизация ✅ ВЫПОЛНЕНА

### Результат
- `src/core/trie-factorizer.ts` — Trie + факторизация:
  - `buildTrie()` / `buildReverseTrie()` — построение Trie
  - `findCommonPrefixes()` / `findCommonSuffixes()` — поиск общих групп
  - `factorize()` — факторизация (prefix, suffix, combined стратегии)
  - `batchFactorize()` — пакетная факторизация по категориям
  - `estimateSavings()` — оценка потенциальной экономии
- `tests/core/trie-factorizer.test.ts` — 40 тестов (Trie, prefix/suffix, factorization, PoE2)

### Контрольная точка ✋ ПРОЙДЕНА
- Trie-factorizer может факторизовать произвольный набор строк
- Тесты покрывают все типы факторизации
- Максимальная вложенность = 3 уровня

---

## 4. Фаза 3: DP на Trie ✅ ВЫПОЛНЕНА

### Результат
- `src/core/dp-factorizer.ts` — DP-факторизация:
  - `dpFactorize()` — главная точка входа, выбирает оптимальную стратегию
  - `dpFactorizeByPrefix()` — DP на префиксном Trie
  - `dpFactorizeBySuffix()` — DP на суффиксном Trie
  - `dpFactorizeCombined()` — DP combined (prefix + suffix extraction)
  - `dpFactorizeGroup()` — рекурсивная факторизация группы строк
  - `applyDialectOptimizations()` — ёфикация `[её]`, окончания `[юя]`
  - `batchDPFactorize()` — пакетная факторизация по категориям
  - `estimateDPCost()` — оценка стоимости без построения регекса
- `tests/core/dp-factorizer.test.ts` — 40 тестов

### Стоимость группировки
- `()` = 2 символа overhead
- `|` = 1 символ на разделитель
- `[]` = 2 символа overhead для character class
- `?` = 1 символ overhead для optional

### Контрольная точка ✋ ПРОЙДЕНА
- DP-factorizer находит оптимальную факторизацию
- DP результат всегда ≤ flat OR по длине
- 40 тестов покрывают все стратегии

---

## 5. Фаза 4: Оптимизации диалекта PoE2 ⬜ ЧАСТИЧНО ВЫПОЛНЕНА

### Выполнено (внутри dp-factorizer.ts)
- ✅ Ёфикация: `[её]` вместо `е|ё` (экономия 2 символа)
- ✅ Окончания: `[юя]` вместо `ю|я`, `[ая]`, `[ые]`, `[ие]`, `[ов]`
- ✅ Top-level merging: `тест|тёст` → `т[её]ст`
- ✅ Within-group: `(е|ё)` → `[её]`

### Не начато (интеграция в ETL)
- ⬜ Опциональные символы: `ь?` вместо `|ь` (экономия 1 символ)
- ⬜ Character class merging: `[а-я]` для диапазонов
- ⬜ Интеграция `applyDialectOptimizations()` в ETL пайплайн

---

## 6-8. Фазы 5-8 ⬜ НЕ НАЧАТЫ

(см. git history для полного описания)

---

## Приложение B: Файловая структура новых модулей

```
src/core/
  trie-factorizer.ts     — Trie + факторизация (Фаза 2) ✅
  dp-factorizer.ts       — DP на Trie + диалект (Фазы 3+4) ✅
  regex-oracle.ts        — Валидатор (Фаза 0) ✅
  number-regex.ts        — ИСПРАВЛЕННЫЙ (Фаза 1) ✅
  poe2-regex-matcher.ts  — без изменений
  optimizer.ts           — обновлён для Trie (Фаза 6) ⬜
  compiler.ts            — без изменений

scripts/etl/
  iterative-optimizer.ts — Итеративный цикл (Фаза 5) ⬜

tests/core/
  trie-factorizer.test.ts  ✅ (40 тестов)
  dp-factorizer.test.ts    ✅ (40 тестов)
  regex-oracle.test.ts     ✅ (25 тестов)
  number-regex.test.ts     ✅ (обновлён)
```

---

## Приложение C: Критерии остановки агента

Агент ДОЛЖЕН остановиться после каждой контрольной точки (✋).

### Формат запроса для продолжения

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru
План: [содержимое этого файла]
Текущая фаза: 4 (интеграция диалектных оптимизаций) / 6 (интеграция DP в ETL)
Что сделано: Фазы 0-3 выполнены. Фаза 4 частично. 318 тестов проходят. DP-factorizer создан.
Команды: cd poe2-regex-ru && pnpm install && pnpm build && npx vitest run --root .
```
