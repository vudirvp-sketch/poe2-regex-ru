# PoE2 Regex RU — План доработок

> **Версия:** 12.0 | **Дата:** 2026-06-07

---

## Текущий статус проекта

- **8 страниц категорий** + VendorPage
- **471 тест** — все проходят
- **Phase 8 реализована частично** — Mixed-conflict exclude + `!(A|B)` формат
- **0 FN** — все моды матчатся
- **ETL re-run (Session 46):** 1490/1573 valid, 83 cross-family FP, 1042 family-tier FP

---

## Оставшиеся задачи

### 🔴 P0 — Критичные

1. **Запустить ETL с Session 47 изменениями** — `pnpm etl -- --validate-item` чтобы увидеть эффект от mixed-conflict exclude + `!(A|B)` + расширенных маркеров. Ожидаемое снижение cross-family FP с 83 до ~50-60.

### 🟡 P1 — Важно

2. **Optimizer expansion** — `compute-optimizations.ts`: пробовать обрезанные формы (truncated) для shared regex в family grouping. Валидировать против всех токенов категории.
3. **Перетест `|` внутри `()`** — Старые тесты могли быть сломаны из-за бага `!"X"`. Переформировать с `"!X"` и перетестить в игре.
4. **Number range с `|`** — `([6-9][0-9]|[0-9][0-9][0-9])` — проверить работает ли в PoE2.

### 🟢 P2 — Желательно

5. **Виртуализация списков** (belt 298, ring 366, amulet 427)
6. **Multi-line mod handling** — Разрушительный: каждая под-строка = отдельный блок в ETL
7. **HomePage hardcoded mod counts** — Category cards show stale counts

---

## Для продолжения в новом чате

```
Продолжи работу над проектом poe2-regex-ru. Репозиторий: https://github.com/vudirvp-sketch/poe2-regex-ru

Итерация 47 (Phase 8 partial) выполнена. 471 тест проходит. pnpm build успешен.

Ключевые изменения Session 47:
- computeExcludePatterns() теперь поддерживает смешанные типы конфликтов (minion + compound + другие)
  - Накапливает короткие маркеры пока все конфликты не покрыты
  - Priority 1: minion "Приспеш" (частичное покрытие OK)
  - Priority 2: compound " и"/"," (частичное покрытие OK)
  - Priority 3: short universal markers (для оставшихся)
  - Priority 4: short first-word excludes (вместо длинных full-phrase)
- !(A|B) комбинированный exclude формат в useCategoryPage.ts
  - Несколько exclude паттернов объединяются в EXCLUDE(OR([...])) → "!A|B" вместо "!A" "!B"
  - Экономит ~3 символа на каждый дополнительный exclude
- Расширен список известных маркеров: "состояния", "заканчив", "воскреш", "во время", "флакона", "умения"

Остановился на:
- P0: Нужно запустить ETL (pnpm etl -- --validate-item) чтобы увидеть реальный эффект Session 47
- P1: Optimizer expansion (compute-optimizations.ts) — пробовать обрезанные формы для family grouping
- P1: Перетест | внутри () с правильным "!X" синтаксисом в игре
- P1: Number range с | — проверить работает ли в PoE2

Ключевые файлы:
- scripts/etl/compute-regex.ts — computeExcludePatterns() с mixed-conflict поддержкой
- src/ui/hooks/useCategoryPage.ts — !(A|B) combined exclude в AST builder
- tests/etl/compute-regex.test.ts — 17 тестов (+2 новых для mixed conflicts)

Команды: cd poe2-regex-ru && pnpm install && npx vitest run --root .
```
